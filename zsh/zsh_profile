if [ -f ~/.zshrc ] ; then
. ~/.zshrc
fi
# rbenv
eval "$(rbenv init -)"
# pyenv
eval "$(pyenv init -)"

# gemのパス 
export PATH="$HOME/.rbenv/shims:$PATH"
# pyenv
# export PYENV_ROOT=/usr/local/var/pyenv
