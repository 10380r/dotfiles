echo Load start .zsh_profile
if [ -f ~/.zshrc ] ; then
. ~/.zshrc
fi

# rbenvのパス
eval "$(rbenv init -)"
# gemのパス 
export PATH="$HOME/.rbenv/shims:$PATH"
# pyenv
export PYENV_ROOT=/usr/local/var/pyenv
if which pyenv > /dev/null ; then eval "$(pyenv init -)"; fi
echo Load finish .zsh_profile
